**Task ID**: M3-005
**Title**: Fix page tree rules so pages can be added under HomePage (not just Root)

**Goal**: Restore the intended Wagtail editor workflow: create the single `HomePage` under Root, then create site pages (e.g. `ServiceIndexPage`) **under HomePage**, with correct parent/child restrictions.

**Workstream**: Page Types
**Milestone**: Milestone 3 – Page Types

**Inputs Required**:

- **PRD Section(s)**: US-P01 (HomePage), US-P02 (Service Pages)
- **Tech Spec Section(s)**: Page type hierarchy / Wagtail conventions (existing repo patterns)
- **Design References**: N/A (admin/tree rules only)
- **Dependencies**: M3-002 (HomePage), M3-004 (Service Pages)

**Files/Modules to Touch**:

- `core/sum_core/test_project/home/models.py` – fix HomePage `subpage_types` so it allows real child page types (not `["wagtailcore.Page"]`).
- `core/sum_core/pages/services.py` – restrict `ServiceIndexPage.parent_page_types` to HomePage only (remove root allowance).
- `tests/pages/test_service_pages.py` (or equivalent) – add regression tests for tree rules.

**Implementation Steps**:

1. **Confirm correct model labels** (avoid guessing strings): in Django shell, print `HomePage._meta.label` and use that exact value in `parent_page_types`.
2. **HomePage child rules**: remove `subpage_types` entirely _or_ replace it with an explicit list of allowed child types (e.g. `ServiceIndexPage`, `StandardPage`, `ContactPage` as they land). Do **not** use `["wagtailcore.Page"]`.
3. **ServiceIndexPage parent rules**: set `parent_page_types` to **only** HomePage (remove `"wagtailcore.Page"`).
4. **Add tests**:

   - `ServiceIndexPage` **can** be created under HomePage
   - `ServiceIndexPage` **cannot** be created under Root

5. **Migrations (mandatory check even if none expected)**:

   - Run `python manage.py makemigrations --check --dry-run` (must be clean)
   - If it reports changes: run `makemigrations`, `migrate`, and commit the migration

6. Run `make test` and ensure full suite passes.
7. Manual admin sanity:

   - Root only offers “HomePage” creation (as intended)
   - HomePage offers “ServiceIndexPage” as an addable child
   - Confirm **Settings → Sites → Root page** points to the HomePage you’re editing (so you’re not fighting the wrong tree).

**Design Guidelines**
N/A (no frontend).

**Acceptance Criteria**:

- [ ] HomePage allows adding valid child pages in Wagtail admin (not “childless”).
- [ ] `ServiceIndexPage` is **not** addable under Root, and **is** addable under HomePage.
- [ ] Automated tests cover both positive + negative tree-rule cases.
- [ ] `makemigrations --check --dry-run` is clean (or migrations were created + migrated + committed).
- [ ] `make test` passes.
